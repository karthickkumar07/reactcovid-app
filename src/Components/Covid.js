import React from "react";

const Covid = ({ cityName }) => {
  return (
    <div className="mt-2">
      <h1 className="text-white">Covid Status In {cityName}</h1>
    </div>
  );
};

export default Covid;
